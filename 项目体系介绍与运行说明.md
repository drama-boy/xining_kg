# 项目体系介绍与运行说明

本文档说明当前知识图谱服务的项目结构、核心流程、配置项和运行方式。

## 一、项目定位

本项目是一个基于 FastAPI 的知识图谱算法服务，主要提供三类能力：

1. 知识图谱构建：接收上传文件，抽取机理事件，生成节点和关系，并可写入 Neo4j。
2. 知识图谱推理：从 Neo4j 读取图谱，对目标实体做风险研判、关联分析、活动规律总结和行为预测。
3. 知识检索：按实体名称从 Neo4j 查询目标节点及其直接关联关系。

## 二、目录结构

```text
xining/
|-- app_server.py                         # FastAPI 服务入口
|-- config.py                             # 配置加载和日志初始化
|-- config/
|   `-- config.yaml                       # 项目配置文件
|-- algorithms/
|   `-- knowledge_graph/
|       |-- build_kg.py                   # 知识图谱构建算法
|       |-- inference.py                  # 图谱推理算法
|       |-- retriever.py                  # 图谱检索封装
|       `-- __init__.py
|-- utils/
|   |-- util.py                           # 文件解析、图谱工具、Neo4j 工具
|   |-- txt_to_json.py                    # 文本到机理事件抽取
|   `-- myllm.py                          # 大模型调用封装
|-- data/
|   |-- temp/                             # 示例或临时输入数据
|   |-- upload/                           # 接口上传文件保存目录
|   `-- output/                           # 图谱和抽取结果输出目录
|-- static/                               # 静态页面目录
|-- logs/                                 # 运行日志目录
`-- requirements.txt                      # Python 依赖
```

## 三、核心模块说明

### 1. `app_server.py`

FastAPI 服务入口，负责：

- 创建 API 应用。
- 创建上传、输出、静态资源和日志目录。
- 暴露接口：
  - `POST /api/knowledge_graph/construction`
  - `POST /api/knowledge_graph/inference`
  - `POST /api/knowledge_graph/retrieve`
  - `GET /`
  - `GET /healthz`
- 保存上传文件到 `data/upload`。
- 调用算法模块并组装接口返回。

### 2. `config.py`

负责读取 `config/config.yaml`，并初始化 `loguru` 日志。

日志默认写入：

```text
logs/app_server_YYYY_MM_DD.log
```

### 3. `algorithms/knowledge_graph/build_kg.py`

知识图谱构建模块，主要流程：

1. 接收输入文件路径。
2. 调用 `utils.util.load_mechanism_events` 解析源文件。
3. 抽取机理事件。
4. 将事件转换为图谱节点、关系和 links。
5. 输出：
   - 图谱文件：`data/output/kg_时间戳.json`
   - 抽取事件文件：`data/output/extracted_xxx.json`
6. 根据配置决定是否写入 Neo4j。

主要函数：

| 函数 | 说明 |
| --- | --- |
| `build_knowledge_graph(input_paths)` | 构建知识图谱主函数 |
| `build_graph_from_files(input_paths)` | 从文件列表解析事件并构图 |
| `kg_wrapper(input_paths)` | 构建函数包装器 |

### 4. `algorithms/knowledge_graph/inference.py`

图谱推理模块，主要流程：

1. 从 Neo4j 读取节点和关系。
2. 根据目标名称匹配实体。
3. 基于实体标签、名称和属性做风险判断。
4. 分析关联对象、事件、行为、时序和地点。
5. 生成活动规律和预测信息。
6. 如果配置启用大模型，则调用 `utils/myllm.py` 生成更自然的总结字段。

主要函数：

| 函数 | 说明 |
| --- | --- |
| `inference_wrapper(target)` | 接口调用的推理包装器 |
| `KGInferenceEngine.from_neo4j()` | 从 Neo4j 加载图谱 |
| `KGInferenceEngine.infer(target)` | 执行目标推理 |

### 5. `algorithms/knowledge_graph/retriever.py`

图谱检索模块，主要流程：

1. 连接 Neo4j。
2. 按 `name` 精确匹配目标实体。
3. 查询目标实体的入边和出边。
4. 返回实体属性和直接关系三元组。

主要函数：

| 函数 | 说明 |
| --- | --- |
| `retrieve_wrapper(query)` | 接口调用的检索包装器 |

### 6. `utils/util.py`

工具模块，提供：

- 文件路径解析。
- txt、csv、pdf、docx、json 文件读取。
- 文本切分。
- 机理事件抽取入口。
- 图谱节点属性标准化。
- 图谱写入 Neo4j。
- Neo4j 实体检索。
- 推理结果格式化工具。

### 7. `utils/myllm.py`

大模型调用封装，使用 OpenAI SDK 兼容接口：

- `llm.base_url`
- `llm.api_key`
- `llm.model`
- `llm.temperature`

当前推理模块会通过 `llm_gemma_31b(prompt)` 调用该服务。

## 四、数据处理流程

### 知识图谱构建流程

```text
上传文件
  ↓
保存到 data/upload
  ↓
按后缀选择解析方式
  ↓
文本切分或 JSON 事件识别
  ↓
txt_to_json 抽取机理事件
  ↓
MechanismKGBuilder 构建节点和关系
  ↓
写出 data/output/*.json
  ↓
按配置写入 Neo4j
  ↓
接口返回图谱统计和 graph_data
```

### 推理流程

```text
输入 target
  ↓
从 Neo4j 加载图谱
  ↓
匹配实体
  ↓
风险研判
  ↓
关联对象和时序分析
  ↓
活动规律统计
  ↓
行为预测
  ↓
可选调用大模型总结
  ↓
接口返回推理结果
```

### 检索流程

```text
输入 query
  ↓
Neo4j 按 name 精确匹配节点
  ↓
查询入边和出边
  ↓
返回实体属性和直接关系
```

## 五、配置说明

配置文件路径：

```text
config/config.yaml
```

### 1. 日志配置

```yaml
log:
  level: INFO
  dir: ./logs
  file_prefix: app_server
  date_format: YYYY_MM_DD
  rotation: 100 MB
  retention: 30 days
```

说明：

| 配置 | 说明 |
| --- | --- |
| `level` | 日志级别 |
| `dir` | 日志目录 |
| `file_prefix` | 日志文件名前缀 |
| `date_format` | 日志文件日期格式 |
| `rotation` | 日志滚动大小 |
| `retention` | 日志保留时间 |

### 2. Neo4j 配置

```yaml
neo4j:
  url: "bolt://localhost:7687"
  user: "neo4j"
  password: "12345678"
```

说明：

| 配置 | 说明 |
| --- | --- |
| `url` | Neo4j Bolt 地址 |
| `user` | Neo4j 用户名 |
| `password` | Neo4j 密码 |

图谱构建、推理和检索都依赖 Neo4j。构建接口在 `knowledge_graph.write_to_neo4j: true` 时会写入 Neo4j；推理和检索接口会直接读取 Neo4j。

### 3. 知识图谱配置

```yaml
knowledge_graph:
  output_prefix: kg
  extracted_prefix: extracted_
  supported_suffixes: [".csv", ".docx", ".json", ".pdf", ".txt", ".png", ".jpg", ".jpeg", ".mp3", ".wav", ".m4a", ".mp4", ".avi", ".mov", ".mkv"]
  write_to_neo4j: true
  clear_neo4j: false
  show_progress: false
  splitter:
    chunk_size: 1800
    chunk_overlap: 120
    separators: ["\n", "。", "？", "；", "!", "！", "?"]
```

说明：

| 配置 | 说明 |
| --- | --- |
| `output_prefix` | 图谱输出文件名前缀 |
| `extracted_prefix` | 抽取事件输出文件名前缀 |
| `supported_suffixes` | 接受的文件后缀 |
| `write_to_neo4j` | 构建后是否写入 Neo4j |
| `clear_neo4j` | 写入前是否清空 Neo4j |
| `show_progress` | 是否显示构建进度 |
| `splitter.chunk_size` | 文本切分块大小 |
| `splitter.chunk_overlap` | 文本切分重叠长度 |
| `splitter.separators` | 文本切分分隔符 |

### 4. 推理配置

```yaml
tuili:
  default_target: T-90S
  use_llm: true
```

说明：

| 配置 | 说明 |
| --- | --- |
| `default_target` | 命令行推理默认目标 |
| `use_llm` | 是否在推理结果中调用大模型生成活动规律和预测描述 |

### 5. 大模型配置

```yaml
llm:
  base_url: http://192.168.1.42:30002/v1
  api_key: xxx
  model: /mnt/gemma-4-26B-A4B-it
  temperature: 0.3
```

说明：

| 配置 | 说明 |
| --- | --- |
| `base_url` | OpenAI 兼容服务地址 |
| `api_key` | API Key |
| `model` | 模型名称 |
| `temperature` | 采样温度 |

### 6. 文件目录配置

```yaml
files:
  data_dir: ./data
  upload_dir: ./data/upload
  output_dir: ./data/output
  temp_dir: ./data/temp
  static_dir: ./static
  model_dir: ./models
  resource_dir: ./data
```

说明：

| 配置 | 说明 |
| --- | --- |
| `upload_dir` | 接口上传文件保存目录 |
| `output_dir` | 图谱结果输出目录 |
| `temp_dir` | 临时数据目录 |
| `static_dir` | 静态页面目录 |

## 六、环境准备

### 1. Python 环境

建议使用 Python 3.10 或更高版本。

创建虚拟环境：

```bash
python -m venv .venv
```

Windows PowerShell 激活：

```powershell
.\.venv\Scripts\Activate.ps1
```

安装依赖：

```bash
pip install -r requirements.txt
```

### 2. Neo4j 环境

需要提前启动 Neo4j，并保证配置文件中的连接信息可用：

```yaml
neo4j:
  url: "bolt://localhost:7687"
  user: "neo4j"
  password: "12345678"
```

如果暂时不希望构建接口写入 Neo4j，可以将：

```yaml
knowledge_graph:
  write_to_neo4j: false
```

但推理接口和检索接口仍然需要 Neo4j 中已有数据。

### 3. 大模型服务

如果 `tuili.use_llm: true`，需要保证 `llm.base_url` 指向的 OpenAI 兼容服务可用。

如果只想使用规则推理，可以将：

```yaml
tuili:
  use_llm: false
```

## 七、启动服务

在项目根目录执行：

```bash
python app_server.py
```

或使用 uvicorn：

```bash
uvicorn app_server:app --host 0.0.0.0 --port 5000
```

启动后访问：

```text
http://127.0.0.1:5000/docs
```

健康检查：

```bash
curl http://127.0.0.1:5000/healthz
```

预期返回：

```json
{
  "status": "healthy"
}
```

## 八、接口调用顺序建议

首次运行建议按以下顺序：

1. 启动 Neo4j。
2. 检查 `config/config.yaml` 中 Neo4j 配置。
3. 启动 FastAPI 服务。
4. 调用知识图谱构建接口上传文件。
5. 确认 `data/output` 生成图谱 JSON。
6. 确认构建结果已写入 Neo4j。
7. 调用检索接口验证实体是否存在。
8. 调用推理接口生成目标分析结果。

## 九、命令行运行算法

### 1. 单独构建知识图谱

```bash
python algorithms/knowledge_graph/build_kg.py --input-files data/temp/example.txt
```

支持多个文件：

```bash
python algorithms/knowledge_graph/build_kg.py --input-files data/temp/example.txt data/temp/example.json
```

### 2. 单独执行推理

```bash
python algorithms/knowledge_graph/inference.py --target T-90S
```

检索模块当前主要作为接口封装使用，也可以直接运行脚本中的默认测试目标：

```bash
python algorithms/knowledge_graph/retriever.py
```

## 十、输出文件说明

### 1. 图谱文件

路径：

```text
data/output/kg_YYYYMMDD_HHMMSS.json
```

结构：

```json
{
  "nodes": [],
  "edges": [],
  "links": []
}
```

### 2. 抽取事件文件

路径：

```text
data/output/extracted_xxx.json
```

内容为从源文件中抽取出的机理事件列表。

### 3. 上传文件

路径：

```text
data/upload/upload_xxx.后缀
```

接口上传的文件会用 UUID 重命名后保存，避免同名覆盖。

## 十一、常见问题

### 1. `/api/knowledge_graph/inference` 或 `/api/knowledge_graph/retrieve` 返回 500

优先检查：

- Neo4j 是否已启动。
- `config/config.yaml` 中账号密码是否正确。
- Neo4j 中是否已有图谱数据。

### 2. 检索接口返回 400

检索使用 Neo4j 节点 `name` 属性精确匹配。请确认查询词与节点 `name` 完全一致。

### 3. 构建接口上传图片、音频、视频失败

当前代码会尝试导入以下转换模块：

- `utils.images_to_json`
- `utils.video_to_json`

如果项目中没有这些模块，相关文件会处理失败。文本、CSV、PDF、DOCX、JSON 文件是当前主要可用输入类型。

### 4. 推理结果中的活动规律或预测为空

可能原因：

- Neo4j 图谱中目标实体关联信息不足。
- `tuili.use_llm: true` 时大模型服务不可访问。
- 目标实体没有被成功匹配。

### 5. 构建接口返回的 `processing_time_s` 为空

当前构建算法内部返回字段名是 `processing_time_ms`，接口层读取的是 `processing_time_s`，所以该字段可能为空。需要统一字段名后才能稳定返回耗时。
