# 接口与返回参数说明

本文档说明当前项目在 `app_server.py` 中暴露的 HTTP 接口、请求参数、返回字段和常见调用方式。

## 基础信息

- 服务框架：FastAPI
- 默认监听地址：`0.0.0.0:5000`
- API 文档地址：启动服务后访问 `http://192.168.1.180:5000/docs`
- 静态输出目录访问前缀：`/output`
- 健康检查接口：`GET /healthz`

说明：当前 `config/config.yaml` 未配置 `server.host` 和 `server.port`，所以服务会使用 `app_server.py` 中的默认值 `0.0.0.0:5000`。

## 通用返回格式

业务接口正常返回时通常包含：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `code` | number | 业务状态码，成功时为 `200` |
| `msg` | string | 业务消息，成功时为 `success` |

异常时 FastAPI 会返回标准错误结构：

```json
{
  "detail": "错误说明"
}
```

常见 HTTP 状态码：

| 状态码 | 说明 |
| --- | --- |
| `200` | 请求处理成功 |
| `400` | 缺少必要参数、文件为空或算法未返回有效结果 |
| `422` | 请求格式不符合 FastAPI 参数校验 |
| `500` | 文件保存、算法处理、Neo4j 或大模型调用等服务端异常 |

## 1. 知识图谱构建

### 接口

```http
POST /api/knowledge_graph/construction
```

### 功能

上传一个或多个源文件，系统会抽取机理事件，构建知识图谱 JSON，并按配置写入 Neo4j。

### 请求格式

`multipart/form-data`

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `files` | file[] | 是 | 上传文件列表。字段名必须为 `files` |

支持的文件后缀来自 `config/config.yaml`：

```text
.csv, .docx, .json, .pdf, .txt, .png, .jpg, .jpeg, .mp3, .wav, .m4a, .mp4, .avi, .mov, .mkv
```

其中当前代码直接支持文本类和 JSON 类文件处理；图片、音频、视频会尝试调用 `utils.images_to_json` 或 `utils.video_to_json` 转换模块，如果项目中没有这些模块，会处理失败并写入失败信息。

### curl 示例

```bash
curl -X POST "http://192.168.1.180:5000/api/knowledge_graph/construction" \
  -F "files=@data/temp/example.txt" 
```

### 成功返回示例

```json
{
  "code": 200,
  "msg": "success",
  "files_count": 2,
  "output_file": "data/output/kg_20260522_110853.json",
  "extracted_file": "data/output/extracted_xxx.json",
  "events_count": 10,
  "nodes_count": 35,
  "edges_count": 42,
  "processing_time_s": "",
  "triples_count": 42,
  "evaluation": {
    "source_files": [],
    "failed_files": [],
    "files_count": 2,
    "imported_to_neo4j": true,
    "write_to_neo4j": true,
    "clear_neo4j": false
  },
  "graph_data": {
    "nodes": [],
    "edges": [],
    "links": []
  }
}
```

### 返回字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `code` | number | 成功时为 `200` |
| `msg` | string | 成功时为 `success` |
| `files_count` | number | 本次接收并保存的上传文件数量 |
| `output_file` | string | 构建完成后的图谱 JSON 文件路径 |
| `extracted_file` | string | 抽取出的机理事件 JSON 文件路径 |
| `events_count` | number | 抽取到的事件数量 |
| `nodes_count` | number | 图谱节点数量 |
| `edges_count` | number | 图谱关系数量 |
| `processing_time_s` | number/string | 接口层返回的处理耗时字段。当前算法内部字段为 `processing_time_ms`，接口未映射时该字段可能为空 |
| `triples_count` | number | 三元组数量，当前等同于关系数量 |
| `evaluation` | object | 构建评估和导入信息 |
| `graph_data` | object | 完整图谱数据 |

### `evaluation` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `source_files` | array | 每个输入文件的处理摘要 |
| `failed_files` | array | 处理失败的文件及错误原因 |
| `files_count` | number | 输入文件数量 |
| `imported_to_neo4j` | boolean | 是否已成功导入 Neo4j |
| `write_to_neo4j` | boolean | 是否按配置启用 Neo4j 写入 |
| `clear_neo4j` | boolean | 写入前是否清空 Neo4j |

### `graph_data` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `nodes` | array | 节点列表 |
| `edges` | array | 关系列表 |
| `links` | array | 面向前端图谱展示的关系列表 |

节点结构：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | number/string | 节点 ID。输出 JSON 中通常为数字 ID |
| `label` | string | 主标签，例如事件、行为、地点、车辆等 |
| `labels` | array | 标签列表 |
| `name` | string | 节点名称 |
| `properties` | object | 节点属性 |

关系结构：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `source` | number/string | 起点节点 ID |
| `relation` | string | 关系类型 |
| `target` | number/string | 终点节点 ID |
| `properties` | object | 关系属性 |

## 2. 知识图谱推理

### 接口

```http
POST /api/knowledge_graph/inference
```

### 功能

基于 Neo4j 中已有图谱，对指定目标实体进行命中、风险研判、关联对象分析、活动规律总结和行为预测。

### 请求格式

`multipart/form-data` 或 `application/x-www-form-urlencoded`

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `target` | string | 是 | 推理目标实体名称，例如 `T-90S` |

### curl 示例

```bash
curl -X POST "http://192.168.1.180:5000/api/knowledge_graph/inference" \
  -F "target=T-90S"
```

### 成功返回示例

```json
{
  "code": 200,
  "msg": "success",
  "target": "T-90S",
  "risk_level": "高危",
  "risk_reason": "命中风险判断依据",
  "timeline": [],
  "relatedobj": {
    "team": [],
    "opponent": []
  },
  "activitypattern": "活动规律描述",
  "forecast": "下一步行为预测",
  "processing_time_s": 1.23
}
```

### 返回字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `code` | number | 成功时为 `200` |
| `msg` | string | 成功时为 `success` |
| `target` | string | 推理目标 |
| `risk_level` | string | 风险等级，例如高危、中危、低危、未知 |
| `risk_reason` | string | 风险等级判断依据 |
| `timeline` | array/string | 与目标相关的时序信息 |
| `relatedobj` | object/string | 关联对象，通常包含 `team` 和 `opponent` |
| `activitypattern` | string | 活动规律总结 |
| `forecast` | string | 行为预测 |
| `processing_time_s` | number/string | 推理耗时，单位秒 |

### 依赖说明

该接口从 Neo4j 读取图谱数据，因此调用前需要：

1. Neo4j 服务可访问。
2. `config/config.yaml` 中 `neo4j.url`、`neo4j.user`、`neo4j.password` 配置正确。
3. 已通过知识图谱构建接口或其他方式将图谱写入 Neo4j。
4. 如果 `tuili.use_llm: true`，需要保证 `llm.base_url` 指向的大模型服务可访问；如果不可访问，接口仍会尽量返回规则推理结果，但大模型生成字段可能为空或使用默认提示。

## 3. 知识检索

### 接口

```http
POST /api/knowledge_graph/retrieve
```

### 功能

从 Neo4j 中按实体名称精确检索目标节点，并返回该节点的属性、入边和出边关系。

### 请求格式

`multipart/form-data` 或 `application/x-www-form-urlencoded`

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `query` | string | 是 | 查询实体名称，例如 `T-90S` |

### curl 示例

```bash
curl -X POST "http://192.168.1.180:5000/api/knowledge_graph/retrieve" \
  -F "query=T-90S"
```

### 成功返回示例

```json
{
  "code": 200,
  "msg": "success",
  "target_entity": "T-90S",
  "entity_info": {
    "name": "T-90S",
    "labels": ["车辆"],
    "properties": {}
  },
  "relationships": [
    {
      "from": "某行为",
      "type": "涉及车辆",
      "to": "T-90S"
    }
  ],
  "processing_time_ms": 12.34
}
```

### 返回字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `code` | number | 成功时为 `200` |
| `msg` | string | 成功时为 `success` |
| `target_entity` | string | 查询目标实体名称 |
| `entity_info` | object/string | 命中节点信息，未命中时算法返回空，接口会转为 400 |
| `relationships` | array/string | 与目标实体直接相连的入边和出边三元组 |
| `processing_time_ms` | number/string | 检索耗时，单位毫秒 |

### `entity_info` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `name` | string | 实体名称 |
| `labels` | array | Neo4j 节点标签 |
| `properties` | object | Neo4j 节点属性 |

### `relationships` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `from` | string | 起点实体名称 |
| `type` | string | 关系类型 |
| `to` | string | 终点实体名称 |

## 4. 根路径

### 接口

```http
GET /
```

### 功能

如果 `static/index.html` 存在，则返回该静态页面；否则返回服务状态 JSON。

### 返回示例

```json
{
  "code": 200,
  "msg": "success",
  "data": "knowledge graph service"
}
```

## 5. 健康检查

### 接口

```http
GET /healthz
```

### 返回示例

```json
{
  "status": "healthy"
}
```

